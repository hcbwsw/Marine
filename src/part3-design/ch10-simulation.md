
# 第7章 多物理域协同仿真体系

> 适用范围：内河船 + 海船电驱动系统
> 覆盖工具：MATLAB/Simulink、AMESim、ANSYS、Maxwell、Fluent、HIL平台
> 内河/海船各50%覆盖

---

## 7.1 仿真体系总体架构

### 7.1.1 为什么需要多物理域协同仿真

船用电驱动系统是典型的**多物理域强耦合系统**，各域之间存在双向耦合：

```
电磁域 ←──────────────────────────────────────► 热域
  铜耗/铁耗 → 热源                               温升 → 绕组电阻升高（铜耗增大）
  电流纹波 → 附加损耗                             磁钢温升 → 退磁风险（PMSM）

热域 ←──────────────────────────────────────► 冷却域
  热源分布 → 冷却需求                             冷却液温度/流量 → 热边界条件
  温升分布 → 热应力                               泵功耗 → 额外热源

机械域 ←──────────────────────────────────────► 控制域
  轴系固有频率 → 控制带宽上限                      速度环带宽 → 扭振激励风险
  扭振幅值 → 编码器信号噪声                        电流纹波 → 转矩波动 → 扭振激励

电磁域 ←──────────────────────────────────────► 机械域
  转矩波动（谐波）→ 轴系激励                       转速波动 → d/q轴电流耦合
  齿槽转矩 → 低速振动                             负载突变 → 电流冲击
```

**如果各域单独仿真，将漏掉以下关键耦合效应：**

| 漏掉的耦合 | 典型后果 | 工程损失 |
|-----------|---------|---------|
| 热-电磁耦合 | 绕组电阻随温升增大8~15%，铜耗估算偏低 | 换热器选小→过热降额 |
| 控制-机械耦合 | 速度环带宽设置过高，激励轴系共振 | 联轴器损坏、振动超标 |
| 冷却-热耦合 | 冷却响应滞后未考虑，热保护触发过频 | 推力损失、乘客投诉 |
| 电磁-机械耦合 | 谐波转矩激励未纳入扭振分析 | 轴系疲劳失效提前 |

### 7.1.2 三层仿真架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│  L3：系统级协同仿真（快速，分钟级，精度±5%）                     │
│  工具：MATLAB/Simulink + Simscape / AMESim 联合仿真              │
│  用途：参数权衡、任务剖面验证、控制策略开发、降额律设计           │
├─────────────────────────────────────────────────────────────────┤
│  L2：子系统级精细仿真（中速，小时级，精度±2%）                   │
│  冷却：AMESim / Flowmaster（管网水力+热网络）                    │
│  传动：ANSYS Mechanical / Simpack（扭振+校中）                   │
│  电机：Maxwell / Flux（电磁场详细模型）                          │
│  控制：Simulink + 嵌入式代码生成（快速控制原型）                 │
├─────────────────────────────────────────────────────────────────┤
│  L1：组件级高保真仿真（慢速，天级，精度±1%）                     │
│  换热器：Fluent / Star-CCM+（CFD 三维流热耦合）                  │
│  轴系：ANSYS Mechanical（疲劳寿命/应力集中）                     │
│  螺旋桨：OpenFOAM / Star-CCM+（水动力CFD）                      │
└─────────────────────────────────────────────────────────────────┘

数据流向：
  L1 → L2：高保真结果作为精确参数（换热系数、Kt/Kq曲线）
  L2 → L3：子系统简化模型参数（热阻、刚度、传递函数）
  L3 ← → HIL：控制器代码在实时环境中验证
```

### 7.1.3 各层仿真输入输出规范

| 仿真层 | 主要输入 | 主要输出 | 精度要求 | 验收准则 |
|-------|---------|---------|---------|---------|
| L3 系统级 | 任务剖面、控制参数、子系统简化模型 | 温升曲线、转速响应、推力、能耗 | ±5°C / ±2%转速 | 与台架试验对比误差≤5% |
| L2 子系统 | 几何参数、材料属性、边界条件 | 流量分布、固有频率、损耗分离 | ±2°C / ±1Hz | 与样机测试误差≤3% |
| L1 组件 | 详细几何、流体参数、工况 | 局部温度场、应力场、流场 | ±1°C / ±0.5% | 与实验室测量误差≤2% |

---

## 7.2 L3 系统级协同仿真模型

### 7.2.1 完整模型架构（Simulink框架）

```
┌─────────────────────────────────────────────────────────────────┐
│                    L3 系统级仿真模型                             │
│                                                                 │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐  │
│  │ 任务剖面  │───►│ 推进管理  │───►│ FOC控制  │───►│ 电机模型  │  │
│  │ 模块     │    │ 模块(PMS)│    │ 模块     │    │ (电磁+热)│  │
│  └──────────┘    └──────────┘    └──────────┘    └──┬───┬──┘  │
│                       │                  ▲           │   │      │
│                       │降额指令           │速度反馈    │   │      │
│                       │                  │           │热源│推力  │
│                  ┌────▼─────┐    ┌───────┴──┐   ┌──▼──┐│      │
│                  │ 热保护&  │    │ 轴系扭振  │   │冷却 ││      │
│                  │ 降额管理 │    │ 模型     │   │模型 ││      │
│                  └──────────┘    └──────────┘   └─────┘│      │
│                                                         │      │
│                                                    ┌────▼────┐  │
│                                                    │螺旋桨负 │  │
│                                                    │载模型   │  │
│                                                    └─────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2.2 模块一：任务剖面模块

```matlab
% 任务剖面定义（内河客船示例）
% 五工况循环，总时长 5.5h/航次

profile = struct();
profile.segments = {
%  工况名称      功率比   持续时间(s)  海温(°C)  备注
  '靠泊等待',    0.05,    1800,        30,      '发动机怠速';
  '低速出港',    0.30,    1200,        30,      '进出港限速';
  '经济航速巡航', 0.75,   14400,       35,      '主要工况';
  '最大航速冲刺', 1.00,    1800,       35,      '赶点补时';
  '倒车靠泊',    0.85,     300,        35,      '靠码头';
};

% 任务剖面向等效工况转换
P_eq = sqrt(sum([P_i.^2 .* t_i]) / sum(t_i));  % 热等效功率
% 用于换热器选型基准（非峰值功率！）
```

**海船任务剖面示例（远洋货船）：**

```
工况段          功率占比    时长占比    海温范围    说明
─────────────────────────────────────────────────────────
港内操纵         15%         5%        20~30°C    拖轮协助、低速
出/进港          35%        10%        20~30°C    狭水道、限速
大洋巡航         80%        70%        5~35°C     主要工况，变海温
恶劣天气减速     50%        10%        5~35°C     波高>5m限速
应急全速          100%       5%        最高设计值  追赶班期/避险

加权等效功率：P_eq = √(0.15²×0.05 + 0.35²×0.10 + 0.80²×0.70
                     + 0.50²×0.10 + 1.00²×0.05) × P_rated
           ≈ 0.756 × P_rated
→ 换热器应按 75.6% 额定功率选型（加30%裕度），而非100%
```

### 7.2.3 模块二：电机电磁-热耦合模型

**Cauer 集总参数热网络（六节点）：**

```
热源分布：
  Q_cu(t) = 3 × I_rms²(t) × R₀ × [1 + α(T_winding - 20°C)]
            ↑ 注意：电阻随温度变化，需迭代求解！
  Q_fe    ≈ K_h×f×B_m^α×V_core + K_e×f²×B_m²×V_core（近似恒定）
  Q_mech  = k_mech × n^1.5（风摩耗，随转速变化）

六节点 Cauer 热网络：

  Q_cu → [R_cu-slot] → T_winding
            ↓
         C_winding × dT_winding/dt = Q_cu - (T_winding-T_slot)/R_cu-slot

  Q_fe → [R_fe-yoke] → T_core
            ↓
         C_core × dT_core/dt = Q_fe - (T_core-T_yoke)/R_fe-yoke

  T_slot → [R_slot-yoke] → T_yoke → [R_yoke-jacket] → T_jacket
            ↓                          ↓
         C_slot × dT_slot/dt        C_yoke × dT_yoke/dt

  T_jacket → [R_jacket-fluid] → T_fluid_in
  T_fluid_out = T_fluid_in + Q_total/(ṁ × Cp)

各热阻参数估算（2000kW电机典型值）：
  R_cu-slot   = 0.05 K/W  （绕组导热脂填充的槽绝缘）
  R_slot-yoke = 0.03 K/W  （槽楔+铁芯接触）
  R_yoke-jacket = 0.02 K/W（铁芯-冷却套接触，含接触热阻）
  R_jacket-fluid = 0.01 K/W（冷却套-冷却液对流，取决于流速）
  
  对流热阻计算：R_conv = 1/(h × A_cooling)
  h = Nu × λ_fluid / D_h  （Nu = 0.023 Re^0.8 Pr^0.4，Dittus-Boelter）
```

**迭代求解流程（热-电磁耦合）：**

```
初始化：T_winding = T_ambient = 25°C

循环迭代（时间步长 Δt = 0.1s）：
  Step 1：根据 T_winding 计算 R_phase(T) = R_20°C × [1+0.00393×(T-20)]
  Step 2：由 FOC 输出 I_dq 计算 Q_cu(t) = 3×I_rms²×R_phase(T)
  Step 3：求解热网络 ODE，得到新的 T_winding, T_core, T_jacket...
  Step 4：更新 Q_cu（铜耗随温升增大约8~12%），回到Step 1
  Step 5：检查收敛：|T_new - T_old| < 0.1°C → 收敛，否则继续

⚠️ 不迭代的后果：100°C温升时铜耗低估约5%，换热器面积偏小，
   实际运行中冷却不足，过热降额频发。
```

### 7.2.4 模块三：冷却回路热网络模型（AMESim/Simulink）

```
状态方程（一维热网络）：

内循环（乙二醇水溶液，50%质量分数）：
  Cp = 3500 J/(kg·K)，ρ = 1060 kg/m³

  质量流量：ṁ_inner = f(n_pump, ΔP_system)  ← 泵曲线查表
  
  热平衡（电机冷却套）：
  T_out_motor = T_in_motor + Q_motor / (ṁ_inner × Cp)
  
  换热器换热量：
  Q_hx = ε × C_min × (T_hot_in - T_cold_in)
  ε = 1 - exp[-NTU×(1-C_r)] / [1 - C_r×exp(-NTU×(1-C_r))]
  NTU = UA / C_min
  
  外循环（海水）：
  T_sea_out = T_sea_in + Q_hx / (ṁ_outer × Cp_sea)

动态仿真关键参数（2000kW系统示例）：
  内循环流量：40~80 L/min（随泵速变化）
  换热器NTU：3.5（清洁状态），2.4（30%污堵）
  热容C_motor：85 kJ/K（集总参数）
  时间常数τ：τ = C_motor × R_th_total ≈ 85000 × 0.04 ≈ 3400s ≈ 57min

→ 电机热时间常数约57分钟！
  意味着：短时过载（<10min）不会立即超温，
          但持续过载必然超温，且冷却响应有显著滞后。
```

### 7.2.5 模块四：轴系扭振简化模型

```
集总参数扭振模型（三质量体系统）：

  [J1]─k1─[J2]─k2─[J3]
  电机   联轴器  螺旋桨

状态空间方程：
  θ̈1 = (T_em - k1(θ1-θ2)) / J1
  θ̈2 = (k1(θ1-θ2) - k2(θ2-θ3)) / J2
  θ̈3 = (k2(θ2-θ3) - T_prop) / J3

固有频率（二自由度近似）：
  ω_n1² = k1(J1+J2)/(J1×J2)
  ω_n2² = k2(J2+J3)/(J2×J3)

典型参数（内河推船 500kW）：
  J_motor  = 8.5 kg·m²（厂家提供）
  J_prop   = 45 kg·m²（CFD估算）
  k_coupling = 2.5×10⁵ N·m/rad（橡胶联轴器）
  
  ω_n1 = √(2.5×10⁵ × (8.5+45)/(8.5×45)) = 89 rad/s → f_n1 = 14.2 Hz
  
  禁速区：n_critical = f_n1×60/Z = 14.2×60/4 = 213 rpm（4叶桨）
  禁速区范围：191~234 rpm（±10%）
```

### 7.2.6 模块五：FOC控制器模型

```matlab
% FOC 控制器 Simulink 模型关键参数

% 电流环（内环）PI 参数
Kp_id = Ld / (5 * Ts);   % d轴电流环比例增益
Ki_id = Rs / (5 * Ts);   % d轴电流环积分增益
Kp_iq = Lq / (5 * Ts);   % q轴电流环比例增益
Ki_iq = Rs / (5 * Ts);   % q轴电流环积分增益

% 电流环带宽验证
BW_current = 1 / (5 * Ts);  % 约为 1/(5×采样周期)，目标200~500Hz

% 速度环（外环）PI 参数
% 关键约束：速度环带宽 < 轴系固有频率 / 3
BW_speed_max = f_n1 / 3;    % 例：14.2Hz/3 = 4.7Hz
Kp_speed = 2*pi*BW_speed * J_total;
Ki_speed = Kp_speed * BW_speed / 10;  % 积分时间常数10倍于带宽

% 扭振陷波滤波器（如需更高速度环带宽）
% 中心频率 = 轴系固有频率，Q值5~10
wn = 2*pi*f_n1;
zeta_notch = 0.05;  % 陷波深度

% 速度给定斜率限制（禁速区穿越）
rate_limit_normal = 10;      % rpm/s（正常加速）
rate_limit_critical = 50;    % rpm/s（穿越禁速区时强制加速）
```

### 7.2.7 模块六：降额与故障管理模块

```
降额逻辑状态机（Simulink Stateflow 实现）：

状态0：正常运行
  条件：T_winding ≤ 130°C AND Q_coolant ≥ Q_min AND 无故障
  输出：P_allow = P_rated（100%）

状态1：热预警
  进入条件：T_winding > 130°C 持续5s
  退出条件：T_winding < 125°C 持续60s → 状态0
  输出：P_allow = P_rated，泵升至100%转速，告警

状态2：热降额
  进入条件：T_winding > 155°C 持续10s
  退出条件：T_winding < 145°C 持续120s → 状态1
  输出：P_allow = P_rated × (170-T_winding)/(170-155)
        （线性降额，170°C时降至0）

状态3：冷却降级
  进入条件：冷却流量 < 60% 额定（主泵故障，备泵运行）
  输出：P_allow = 0.80 × P_rated

状态4：紧急停机
  进入条件：T_winding > 175°C OR 过流 > 1.5×额定
  输出：P_allow = 0，驱动使能关断，保留冷却泵

前馈优化（新增）：
  P_pred_loss = P_cmd × (1 - η_motor(n, T))
  Q_pump_pred = f(P_pred_loss)  ← 根据预测损耗提前调节泵速
  效果：热响应滞后从60s→5s，降额频率降低约80%
```

---

## 7.3 L2 子系统级精细仿真

### 7.3.1 冷却管网水力仿真（AMESim）

**管网建模要素：**

```
元素类型        参数需求                    数据来源
──────────────────────────────────────────────────────
泵              Q-H特性曲线（3~5条等速线）    厂家性能曲线
管路            长度、内径、粗糙度、弯头数   设计图纸
换热器          ε-NTU特性、压降特性          厂家数据或L1仿真
阀门            Cv系数、开度-流量关系        厂家数据
冷却套          流道尺寸、表面积             电机详细图纸
膨胀水箱        体积、充气压力              设计规格
```

**内河船管网仿真工况矩阵（必须覆盖全部工况）：**

| 工况 | 主泵状态 | 备泵状态 | 外循环状态 | 海温 | 验证目标 |
|------|---------|---------|----------|------|---------|
| 正常额定 | 100% | 待机 | 100% | 35°C | 温升≤80K |
| 正常经济 | 75% | 待机 | 75% | 35°C | 效率最优点 |
| 主泵故障 | 0% | 100% | 100% | 35°C | 降额后温升≤80K |
| 换热器30%污堵 | 100% | 待机 | 100% | 35°C | 温升≤90K（允许降额） |
| 冬季低温 | 50% | 待机 | 50% | 5°C | 最低温度≥5°C（防冻） |
| 怠速靠泊 | 30% | 待机 | 30% | 30°C | 泵不汽蚀 |

**典型仿真结果分析（内河船 2000kW）：**

```
正常额定工况（T_sea=35°C，P=100%额定）：
  内循环流量：72 L/min（设计值80 L/min，工作点偏移8%）
  泵工作压差：65 kPa（设计扬程7.5m）
  换热器效率ε：0.87（清洁）
  电机入口温度：43°C（目标≤45°C）✅
  绕组温升：76K（目标≤80K）✅
  泵效率：74%（目标≥70%）✅

主泵故障，备泵运行：
  内循环流量：58 L/min（降至73%）
  系统自动降额至82%功率
  绕组温升：79K（仍满足≤80K）✅
  → 降级运行能力验证通过
```

### 7.3.2 扭振详细仿真（Simpack/ANSYS）

**扭振分析完整流程：**

```
Step 1：轴系几何建模
  - 电机转子：转动惯量J（厂家提供或用CAD计算）
  - 各轴段：GIp/L（G=80GPa，Ip=πd⁴/32，L=轴段长度）
  - 联轴器：等效扭转刚度k_c（厂家提供，含温度修正）
  - 螺旋桨：J_prop（CFD估算，加附加水质量效应）

Step 2：附加水质量效应（海船螺旋桨必须考虑）
  J_prop_wet = J_prop_dry × (1 + k_added_mass)
  k_added_mass ≈ 0.20~0.25（4叶桨经验值）
  → 水中固有频率比空气中低约10%！
  ⚠️ 反例：某项目用干态固有频率设计禁速区，
     实际螺旋桨入水后固有频率降低11%，
     禁速区设置偏高，实际共振点在"安全区"内，
     海试发现强烈振动后紧急修改控制参数。

Step 3：激励谐波分析
  电机电磁激励（PMSM）：
    主要激励阶次：6k（k=1,2,3...）→ 6倍频为最主要
    幅值：T_6th ≈ 5%×T_rated（典型值，需电磁仿真确认）
  
  螺旋桨水动力激励：
    激励阶次：Z（叶片数）和 Z的整数倍
    幅值：与螺旋桨不均匀来流有关（伴流场）
    海船：伴流不均匀度高 → 激励幅值大
    内河：水浅、流速均匀 → 激励幅值相对小

Step 4：Campbell图绘制
  横轴：转速（rpm），纵轴：频率（Hz）
  固有频率线：水平线（f_n1, f_n2...）
  激励线：斜线（f = order × n/60）
  交叉点 ± 10% 转速范围 = 禁速区

Step 5：强迫振动响应计算
  τ_dynamic = T_excitation × Q_factor（共振放大因子）
  Q ≈ 1/(2ζ)（ζ=阻尼比，典型0.02~0.05）
  τ_total = τ_static + τ_dynamic ≤ τ_allowable / 1.5（安全系数）
```

**海船扭振案例（4000kW渡船）：**

```
初始设计：
  联轴器刚度：5×10⁵ N·m/rad（过高）
  一阶固有频率：f_n1 = 18.5 Hz
  4叶桨交叉点：n = 18.5×60/4 = 277.5 rpm
  额定转速：280 rpm → 共振点就在额定转速附近！

解决方案：
  Option A：更换软联轴器（刚度降至2×10⁵ N·m/rad）
    f_n1降至11.8Hz，共振点=177rpm（低于最低运行转速）✅
  Option B：增加飞轮（增大J_motor）
    增重200kg，成本高，不推荐
  Option C：设置禁速区（250~305rpm）
    需要快速穿越该区域，操纵不便，不推荐

选择Option A，更换联轴器后验证：
  Campbell图无禁区交叉（额定转速280rpm以上）✅
  强迫振动响应计算：τ_dynamic/τ_static = 0.08（8%，可接受）✅
```

### 7.3.3 电磁场仿真（Maxwell 2D/3D）

**主要仿真任务：**

```
任务1：损耗精确计算（用于热网络参数标定）
  铜耗：Q_cu = 3×I²×R（含集肤效应修正，高频时铜耗增大20~50%）
  铁耗：分离为磁滞损耗+涡流损耗+附加损耗
  输出：损耗密度分布图 → 热网络各节点热源修正

任务2：退磁风险评估（PMSM海船电机）
  工况：温度150°C + 三倍额定电流（堵转工况）
  验证：磁钢最低磁密度 > 不可逆退磁磁密度（Bd）
  结论：如存在退磁风险，必须限制堵转电流或降低磁钢工作温度

任务3：谐波转矩分析（扭振激励输入）
  输出：各次谐波转矩幅值（T_6th, T_12th...）
  作为扭振强迫振动响应计算的激励输入

任务4：齿槽转矩（低速运行特性）
  内河船低速进出港（30rpm以下）时，齿槽转矩引起转速波动
  目标：齿槽转矩 ≤ 1% T_rated（采用分数槽绕组可降低）
```

---

## 7.4 L1 组件级高保真仿真

### 7.4.1 换热器 CFD 仿真（Fluent/Star-CCM+）

**板式换热器 CFD 建模要点：**

```
计算域：
  - 单流道（利用周期对称性简化）或完整换热器
  - 网格：六面体结构网格，边界层y+≈1（高精度壁面处理）
  - 网格数量：单流道约 50~200 万，完整换热器约 1000 万

边界条件：
  内侧（乙二醇）：入口温度65°C，流量按单流道均分
  外侧（海水）：入口温度35°C，流量按单流道均分
  壁面：无滑移，流-固耦合导热

湍流模型：
  Re < 2300（层流）：层流模型
  Re > 4000（湍流）：Realizable k-ε 或 SST k-ω
  过渡区：γ-Reθ 转捩模型

输出结果用途：
  换热系数 h → 修正 L3 热网络中 R_jacket-fluid
  压降 ΔP   → 修正 L2 管网水力模型中换热器阻力
  温度均匀性 → 识别流量分布不均导致的局部过热

典型结果（海水型板式换热器，80kW换热量）：
  换热系数：h_inner=4800 W/(m²K)，h_outer=6200 W/(m²K)
  总传热系数：U = 1800 W/(m²K)
  压降（内侧）：22 kPa，压降（外侧）：18 kPa
  换热面积需求：A = Q/(U×ΔT_lm) = 80000/(1800×22) = 2.02 m²
  选型：A=2.6 m²（含30%污堵裕度）
```

### 7.4.2 螺旋桨水动力 CFD（OpenFOAM/Star-CCM+）

**仿真目的与方法：**

```
目的1：获取精确的Kt、Kq曲线（替代系列桨插值）
  重要性：设计点偏离系列桨范围时（如浅吃水/大盘面比），
          系列桨误差可达15%，CFD误差<3%

目的2：非均匀来流下的螺旋桨激励力（海船伴流场）
  重要性：扭振激励的主要来源，均匀来流假设误差很大

仿真方法：
  稳态（BEP特性）：旋转参考系，多工况扫描J=0.2~0.8
  非定常（激振力）：滑移网格，时间步对应1°~2°转角
  伴流场：先用无螺旋桨模型算船体伴流，再耦合螺旋桨

内河船简化策略：
  浅水效应修正：kt_shallow = Kt × f(h/D)（h=水深，D=桨径）
  浅水时Kt增大约5~15%（h/D<2时显著）
  → 内河浅水区螺旋桨推力预测必须加浅水修正！
```

---

## 7.5 HIL（硬件在环）仿真平台

### 7.5.1 HIL 平台架构

```
┌─────────────────────────────────────────────────────────────┐
│                     HIL 平台架构                             │
│                                                             │
│  ┌──────────────┐          ┌──────────────────────────────┐ │
│  │  实物控制器  │          │     实时仿真机（dSPACE/      │ │
│  │  （被测件）  │          │     Speedgoat/NI PXI）       │ │
│  │              │          │                              │ │
│  │  推进控制器  │◄────────►│  电机模型（电磁+热）         │ │
│  │  (PDC)      │  I/O信号 │  冷却系统模型                │ │
│  │  变频器控制板│          │  轴系扭振模型                │ │
│  │  热保护逻辑  │          │  螺旋桨负载模型              │ │
│  └──────────────┘          │  故障注入模块                │ │
│         │                  └──────────────────────────────┘ │
│         │ CAN/EtherCAT                      │               │
│  ┌──────▼──────┐                    ┌───────▼──────┐        │
│  │  监控与数据  │                    │  故障注入    │        │
│  │  采集系统   │                    │  控制台      │        │
│  │  (LabVIEW)  │                    │              │        │
│  └─────────────┘                    └──────────────┘        │
└─────────────────────────────────────────────────────────────┘

关键指标：
  实时步长：100μs~1ms（电流环）；10ms（热模型）
  I/O延迟：<100μs
  模型精度：与台架试验对比误差<3%
```

### 7.5.2 HIL 测试用例矩阵（完整版）

**必须完成的最小 HIL 测试用例集（共20个）：**

| 编号 | 测试用例 | 被测功能 | 验收准则 | 内河/海船 |
|------|---------|---------|---------|---------|
| HIL-01 | 额定工况稳态运行 | 控制精度 | 转速稳态误差≤0.5% | 两者 |
| HIL-02 | 10%转速阶跃响应 | 速度环带宽 | 调节时间≤设计值，超调≤5% | 两者 |
| HIL-03 | 负载突变（75%→100%）| 抗扰性 | 转速跌落≤3%，恢复时间≤2s | 两者 |
| HIL-04 | 禁速区穿越 | 禁速区控制 | 穿越时间≤4s，无停留 | 两者 |
| HIL-05 | 倒车工况 | 四象限运行 | 转向平滑，无冲击（dT/dt≤限值）| 两者 |
| HIL-06 | 热保护Level 1 | 预警逻辑 | 130°C告警，泵升速，不降额 | 两者 |
| HIL-07 | 热保护Level 2 | 降额逻辑 | 155°C开始线性降额，公式正确 | 两者 |
| HIL-08 | 热保护Level 3 | 停机逻辑 | 175°C停机≤0.5s，冷却泵保留 | 两者 |
| HIL-09 | 温度传感器单路失效 | 传感器容错 | 切换至备用路，触发告警 | 两者 |
| HIL-10 | 温度传感器双路失效 | 失效安全 | 触发降额至60%，不停机 | 两者 |
| HIL-11 | 内循环泵故障切换 | 冗余切换 | 5s内切换至备泵，告警 | 两者 |
| HIL-12 | 冷却流量低于60% | 冷却降级 | 自动降额至80%，告警 | 两者 |
| HIL-13 | 通信总线中断 | 通信容错 | 10s内检测，切至本地安全模式 | 两者 |
| HIL-14 | 控制电源失电再恢复 | 上电逻辑 | 自检通过后平滑恢复，无冲击 | 两者 |
| HIL-15 | 编码器信号丢失 | 编码器容错 | 切至无传感器控制或安全停机 | 两者 |
| HIL-16 | 全航次任务剖面仿真 | 综合性能 | 温升满足要求，能耗符合预测 | 内河 |
| HIL-17 | 大洋航行变海温 | 变工况适应 | 5~35°C海温下全程满足KPI | 海船 |
| HIL-18 | 恶劣天气减速 | 快速功率变化 | 功率50%→80%阶跃，稳定过渡 | 海船 |
| HIL-19 | E-STOP紧急停止 | 安全功能 | 0.5s内停止输出，硬线验证 | 两者 |
| HIL-20 | 多故障组合（泵故障+传感器故障）| 组合故障 | 进入预定降级状态，不崩溃 | 两者 |

### 7.5.3 故障注入测试方法

```
故障注入方式：
  硬件故障注入：通过继电器断开传感器信号线（模拟断路）
  软件故障注入：实时仿真机发送异常信号值（模拟漂移/卡死）
  网络故障注入：通过网络故障模拟器制造丢包/延迟

典型故障注入脚本（HIL-10示例）：

t=0s：系统正常额定运行（100%功率，转速稳定）
t=10s：注入温度传感器A断路故障（信号=0或开路）
        → 验证：系统检测到A路断路，切换至B路，触发告警
t=30s：注入温度传感器B断路故障（双路均失效）
        → 验证：系统在3s内检测到双路失效，
                自动降额至60%功率（失效安全）
                故障代码正确，HMI显示正确告警
t=60s：恢复传感器A信号正常
        → 验证：系统识别A路恢复，但保持当前状态，
                等待人工确认后才能提升功率（防误操作）

验收准则：
  ① 双路失效检测时间 ≤ 3s ✅
  ② 降额至60%的过渡时间 ≤ 5s（无冲击）✅
  ③ 故障代码：0x2031（温度传感器双路失效）✅
  ④ HMI显示："温度监测失效，推力降级运行，请联系维修" ✅
  ⑤ 功率稳定在60%额定，无振荡 ✅
```

---

## 7.6 仿真与试验相关性标定

### 7.6.1 标定流程（三步法）

```
Step 1：台架试验采集基准数据
  试验工况矩阵（至少覆盖以下组合）：
  
  负载点：30% / 50% / 75% / 100% 额定功率
  冷却流量：最小/额定/最大流量
  环境温度：15°C / 25°C / 35°C（模拟不同海温）
  
  测量量：
  - 绕组温升（每相，PT100）
  - 冷却套进出口温差
  - 损耗（输入功率-输出功率，精度0.1%）
  - 各点压差和流量
  - 振动加速度（轴承座）

Step 2：模型参数辨识
  热网络参数辨识：
    R_winding_slot：由绕组-铁芯温差 ÷ 铜耗估算
    R_yoke_jacket ：由铁芯-冷却套温差 ÷ 铁耗估算
    C_thermal    ：由温升时间常数 τ = R×C 拟合
  
  优化方法：最小二乘法拟合仿真曲线到试验数据
  目标函数：min Σ(T_sim_i - T_test_i)²

Step 3：标定验证（独立验证工况）
  选取与标定工况不同的3个验证工况
  验收准则：
    温升预测误差 ≤ ±5°C（L3模型）
    损耗预测误差 ≤ ±2%（L2模型）
    固有频率预测误差 ≤ ±5%（扭振模型）
```

### 7.6.2 标定结果示例（内河船 2000kW 电机）

```
标定前 vs 标定后（100%额定负载，T_sea=35°C）：

参数          试验实测值    标定前仿真   误差    标定后仿真   误差
────────────────────────────────────────────────────────────────
绕组温升       76.3K        82.1K       +7.6%   75.8K       -0.7% ✅
冷却套出口温度  63.7°C       67.2°C      +5.5%   63.9°C      +0.3% ✅
总损耗         82.4kW       78.1kW      -5.2%   82.8kW      +0.5% ✅
内循环流量     71.3 L/min   74.0 L/min  +3.8%   71.8 L/min  +0.7% ✅

标定关键修正项：
  R_yoke_jacket：初值0.025 K/W → 标定值0.031 K/W（接触热阻比预期大24%）
  原因：铁芯与冷却套之间的装配间隙导热硅脂填充不均匀
   → 工艺改进：规定硅脂涂布规程，确保均匀接触
```

---

## 7.7 仿真验证报告与VCRM的关联（自动化关闭证据）

### 7.7.1 问题描述

当前状态：
- 第7章生成多份仿真报告（HIL报告、Simulink报告、AMESim报告）
- 第14章VCRM中有"S类"验证条目（仿真验证）
- 但两者**没有显式的对应关系**，工程师需要手工查找

结果：
- VCRM中的S类条目难以自动关闭（无法追溯到具体仿真报告）
- 验证完成度难以统计（不知道哪个仿真报告对应哪条需求）
- 风险：重复仿真或遗漏验证

**本节建立**：
仿真报告 ←→ VCRM条目 的**双向追踪表**，使验证链路清晰可追溯

### 7.7.2 仿真报告的标准化结构

所有仿真报告应包含以下标准章节（与VCRM对接）：

**表 7.7-1：仿真报告标准结构**

| 报告章节 | 内容 | VCRM对接 |
|---------|------|---------|
| **Executive Summary** | 一句话说清"验证了什么"，列出关闭的VCRM编号 | REQ-COOL-001, REQ-CTRL-003,... |
| **Model Description** | 模型范围、精度、参数来源 | 关联第4/5/6章设计文件 |
| **Verification Results** | 逐个工况的验证结果，每个结果关联1个VCRM编号 | 表7.7-2（结果对照表） |
| **Limitations** | 本报告**不能**验证的VCRM条目及原因 | 后续需其他验证方法 |
| **Conclusions** | 总体结论：VCRM S类条目关闭率XX% | 后续行动 |

### 7.7.3 仿真验证结果对照表（模板）

**表 7.7-2：HIL验证场景与VCRM的对应关系示例**

| 仿真工况 | 验证内容 | VCRM编号 | 验证标准 | 结果 | 证据位置 |
|---------|---------|----------|---------|------|---------|
| HC-01：正常启停 | 启动延迟≤3s | REQ-CTRL-001 | ≤3s | ✅PASS | HIL报告§3.1.2 |
| HC-03：稳速巡航 | 温度稳定±2°C | REQ-COOL-001 | ±2°C | ✅PASS | AMESim报告Fig.4 |
| HC-03：稳速巡航 | 功率精度±3% | REQ-PERF-001 | ±3% | ✅PASS | Simulink报告Tab.5 |
| HC-06：温度传感器故障 | 故障检测时间≤3s | REQ-CTRL-050 | ≤3s | ✅PASS | HIL报告§4.2.1 |
| HC-10：E-STOP响应 | 推力→0响应时间≤100ms | REQ-SAFE-001 | ≤100ms | ✅PASS | HIL报告§5.1 |
| **统计** | **HIL场景覆盖** | **VCRM S类** | **关闭率** | **85%** | **待FAT补充15%** |

### 7.7.4 VCRM中S类条目的自动关闭规则

**原则**：仿真验证结果直接关闭VCRM

**实施方式**：

在VCRM（第14章10.2节表）中新增"证据文件"列，格式统一为：
```
[报告类型]-[报告编号]-[章节号]
例如：HIL-001-3.1.2（表示"HIL报告001号第3.1.2节"）
```

**VCRM关闭流程**：

```
Day 70: HIL验证完成
  ↓
生成HIL报告 v1.0
  ├─ Executive Summary列出关闭的VCRM编号（≥30条S类）
  ├─ 表7.7-2中填写对应关系
  └─ 版本号同时更新为"v1.0"
  ↓
Day 75: VCRM更新
  执行：对表7.7-2中所有"✅PASS"的行，
       在VCRM中定位对应的VCRM编号
       状态改为：✅CLOSED（关闭）
       证据链接到："HIL-001-§3.1.2"
  ↓
Day 76: 统计
  VCRM自动计算：S类关闭率 = CLOSED条目数 / 总S类条目数
  例如：85/100 = 85%
  ↓
Day 85: SIT验证再次关闭
  SIT报告补充关闭：剩余15条S类条目
  VCRM S类总关闭率 = 100%
```

### 7.7.5 三类仿真报告的VCRM对接模式

#### L3系统级仿真报告（Simulink+AMESim联仿）

**验证对象**：系统级性能（热平衡、功率、响应时间）

**VCRM对接**：
- 覆盖REQ-PERF-xxx、REQ-COOL-xxx、REQ-CTRL-001~005等**基础功能需求**
- 预计关闭VCRM S类条目：≥20条
- 报告名称：`SYSTEM-SIM-v1.0.pdf` (日期：第7周)

**报告Executive Summary示例**：
```
本报告通过联合仿真验证了以下需求：
✅ REQ-PERF-001: 推进功率 [Simulink输出数据]
✅ REQ-COOL-001: 冷却温度范围 [AMESim热模型]
✅ REQ-CTRL-001: 启动延迟 [Simulink FOC控制]
...
总计：23条VCRM S类需求已CLOSED
```

#### L2子系统级精细仿真（专项计算）

**验证对象**：子系统特殊功能（扭振、EMC、热失控）

**VCRM对接**：
- 覆盖REQ-SHAFT-xxx（扭振）、REQ-EMC-xxx、REQ-BAT-xxx（热失控）等**专项需求**
- 预计关闭VCRM S类条目：≥15条
- 报告名称：`TORSION-SIM-v1.0.pdf`、`EMC-SIM-v1.0.pdf`、`THERMAL-RUNAWAY-SIM-v1.0.pdf`

**VCRM对接表示例**：
```
| 需求编号 | 验证方法 | 仿真报告 | 证据位置 |
|---------|---------|---------|---------|
| REQ-SHAFT-001 | 分析+仿真 | TORSION-SIM-v1.0 | Fig.2 (Campbell图) |
| REQ-EMC-001 | 仿真 | EMC-SIM-v1.0 | §3.1 (谐波含量) |
```

#### HIL实时仿真报告（控制软件验证）

**验证对象**：控制逻辑、故障处理、实时性能

**VCRM对接**：
- 覆盖REQ-CTRL-xxx、REQ-SAFE-xxx（安全功能）、REQ-DIAG-xxx（诊断）
- 预计关闭VCRM S类条目：≥40条（最多）
- 报告名称：`HIL-001-v1.0.pdf` (日期：第9周)

**报告结构**：
```
HIL测试报告 v1.0
├─ Executive Summary
│  └─ 关闭VCRM条目：HC-01[REQ-CTRL-001,002]...HC-18[REQ-999]
│     总计：42条VCRM S类条目CLOSED
│
├─ 测试场景HC-01~HC-18详细结果
│  └─ 每个场景后跟"VCRM对应表"
│
├─ 故障注入(FI-01~FI-10)结果
│  └─ 故障注入FI-05响应时间<100ms → REQ-SAFE-001 ✅PASS
│
└─ VCRM关闭总表
   └─ 汇总所有42条PASS条目，标注证据位置
```

### 7.7.6 仿真报告的版本管理与VCRM同步

**时间节点**：

```
Day 50: Simulink模型v1.0完成
  ↓ 生成系统仿真报告 SYSTEM-SIM-v1.0
  ↓ VCRM更新：20条S类条目 CLOSED
  ↓ VCRM关闭率(S类) = 20/85 = 23%

Day 60: AMESim模型v1.0完成
  ↓ 生成热仿真报告 THERMAL-SIM-v1.0
  ↓ VCRM更新：额外8条S类条目 CLOSED
  ↓ VCRM关闭率(S类) = 28/85 = 33%

Day 70: HIL v1.0验证完成
  ↓ 生成HIL报告 HIL-001-v1.0
  ↓ VCRM更新：42条S类条目 CLOSED
  ↓ VCRM关闭率(S类) = 70/85 = 82%

Day 85: SIT台架验证完成
  ↓ 补充关闭：15条S类条目
  ↓ VCRM关闭率(S类) = 85/85 = 100% ✅
```

### 7.7.7 检查清单

```
□ 所有仿真报告都有"Executive Summary"章节
□ Executive Summary中明确列出关闭的VCRM编号
□ 所有仿真结果表(如7.7-2)中都有VCRM编号关联
□ VCRM中S类条目都能追踪到对应的仿真报告
□ 仿真报告版本号与VCRM更新时间保持同步
□ 无"孤立的VCRM条目"（S类仍为Open且无仿真证据）
```

---

## 7.8 仿真数据管理与版本控制

### 7.8.1 仿真模型版本管理规范

```
版本命名规则：
  [项目代号]-[子系统]-[版本号]-[日期]
  例：SHIP2024-COOL-v2.3-20241115.slx

版本控制要求：
  工具：Git（结合 Git-LFS 处理大型仿真文件）
  分支策略：
    main分支：已验证、与试验对标通过的基准模型
    dev分支：开发中的参数优化
    feature分支：新功能或新架构探索
  
  每次提交必须包含：
    ① 修改说明（修改了什么参数/结构，为什么修改）
    ② 关联的需求ID或FMEA条目ID
    ③ 验证状态（未验证/部分验证/已完全验证）

仿真报告归档要求：
  每个重要仿真节点生成正式报告，包含：
  ① 仿真目的和输入条件
  ② 关键结果（图表+数值）
  ③ 与设计要求的对比
  ④ 结论和设计建议
  ⑤ 模型版本号和仿真日期
```

### 7.8.2 仿真置信度评估矩阵（新增）

| 仿真类型 | 模型验证状态 | 置信度 | 允许用途 | 不允许用途 |
|---------|-----------|--------|---------|---------|
| L3系统级（未标定）| 无试验对标 | 低（60%）| 概念阶段权衡 | 最终设计决策 |
| L3系统级（已标定）| 误差<5% | 高（90%）| 设计验证、控制开发 | 免试验替代 |
| L2子系统（已验证）| 误差<3% | 高（92%）| 参数选型、FMEA支撑 | 完全替代台架 |
| L1组件CFD（已验证）| 误差<2% | 高（95%）| 精确参数提取 | 样机鉴定 |
| HIL（控制器实物）| 软件在环 | 中（85%）| 控制逻辑验证 | 机械故障验证 |

---

## 7.9 典型工程案例

### 7.9.1 内河客船案例：仿真发现换热器选型错误

```
背景：某内河电推客船，2×1000kW双轴推进，设计阶段仅进行热计算，
      未做动态仿真，换热器按单一额定工况选型。

问题发现（仿真阶段）：
  仿真工况：靠泊→出港→巡航→最大速度完整任务剖面动态仿真
  
  发现1：靠泊低功率（5%额定）阶段，冷却液温度降至28°C，
         换热器出力不足（偏低），泵工作点严重偏离高效区
         泵效率：42%（设计值78%）→ 泵长期欠载运行，寿命降低
  
  发现2：从靠泊→最大功率（5min内功率从5%到100%），
         热响应滞后约8分钟，绕组温升峰值92K（超过F级80K！）
         静态仿真显示最终稳态78K（看似安全），
         但动态仿真暴露了瞬态峰值超标问题。

解决方案：
  ① 换热器缩小15%面积（按等效任务剖面功率选型）
  ② 泵变频调速（根据功率指令调节泵速）
  ③ 前馈控制：功率指令发出同时预调泵速，热响应从8min→2min
  
成本分析：
  仿真发现时：增加变频泵控制器约3万元，换热器优化节省1.5万元
  台架试验发现：需重新选型换热器+重新安装，约15万元+延期1个月
  海试发现：返厂修改，约80万元+延期3个月
  → 仿真阶段发现节省约78万元工程损失
```

### 7.9.2 海船案例：扭振仿真避免轴系共振

```
背景：某4000kW海船电推，设计转速280rpm，4叶桨。
      项目方认为"参考同类船型不做扭振分析"。

仿真分析（要求补做）：
  一阶固有频率（考虑附加水质量）：f_n1 = 19.2 Hz
  4叶桨激励频率：f_exc = 4×280/60 = 18.7 Hz ≈ f_n1！
  → 额定转速附近存在严重共振风险！

如果不做仿真直接投产：
  预计结果：强烈轴系振动，联轴器橡胶元件1~2个月撕裂，
            振动超过船级社限值，无法通过海试

解决方案（仿真后提前处理）：
  联轴器刚度降低35%→ f_n1=15.1Hz，共振点降至227rpm
  电控增加225~232rpm禁速区（穿越时间<3s）
  
节省成本：
  设计变更（联轴器重选）：约2万元，提前2周处理
  vs 投产后返工：整套轴系拆解+重装+海试，约200万元+延期2个月
```

---

## 7.10 仿真体系建设路线图

### 7.10.1 分阶段建设计划（适合新入行企业）

```
阶段一（0~6个月）：基础能力建设
  工具：Simulink + Simscape（已有许可证优先）
  建立：电机简化热模型（三节点）
  建立：冷却回路稳态计算模型
  目标：能够完成稳态热设计计算，替代手工Excel计算
  人员：1名仿真工程师，培训周期3个月

阶段二（6~18个月）：动态仿真能力
  工具：AMESim（管网水力）+ Simpack（扭振）
  建立：冷却系统动态仿真模型（已与台架试验标定）
  建立：轴系扭振完整分析能力
  建立：L3系统级协同仿真模型（热+控+机械）
  目标：能够支撑完整的动态设计验证
  人员：2名仿真工程师

阶段三（18~36个月）：HIL与高保真仿真
  工具：dSPACE/Speedgoat HIL平台 + Fluent（CFD）
  建立：HIL平台（实物控制器+虚拟对象）
  建立：换热器CFD仿真能力（组件验证）
  建立：完整的仿真-试验相关性标定体系
  目标：能够大幅减少台架试验轮次（缩短30%开发周期）
  人员：3~4名仿真工程师，1名HIL工程师
```

### 7.10.2 仿真能力成熟度评估（CMM）

| 成熟度级别 | 描述 | 典型能力 | 目标周期 |
|-----------|------|---------|---------|
| Level 1（初始）| 靠人工Excel，无仿真 | 手工稳态热计算 | 现状 |
| Level 2（受管理）| 有仿真工具，非系统化 | 单域仿真，未与试验对标 | 6个月 |
| Level 3（已定义）| 有标准仿真流程，已标定 | 多域协同仿真，流程规范 | 18个月 |
| Level 4（量化管理）| 仿真结果可预测，有置信度 | HIL验证，仿真替代部分试验 | 36个月 |
| Level 5（优化）| 仿真驱动设计，持续改进 | 实时仿真、数字孪生 | 5年 |

---

## 7.11 本章检查单

```
仿真体系建设检查单（CDR阶段必须完成）：

L3 系统级仿真：
□ 任务剖面模型建立，覆盖全部工况段（≥5个工况）
□ 电机热网络模型建立（至少4节点）
□ 冷却回路动态模型建立（含泵曲线、换热器ε-NTU）
□ FOC控制器模型建立（含热保护降额逻辑）
□ 轴系扭振简化模型建立（至少3质量体）
□ L3模型与台架试验对标完成，误差≤5°C / ≤2%转速

L2 子系统仿真：
□ 冷却管网水力仿真完成（全部工况矩阵覆盖）
□ 扭振详细分析完成，Campbell图无禁区交叉
□ 电磁场仿真完成（损耗分离+谐波转矩）
□ 退磁风险评估完成（PMSM电机）

HIL 验证：
□ HIL 平台搭建完成（实时步长≤1ms）
□ 20个必须测试用例全部执行完毕
□ 所有不合格项关闭并回归测试通过
□ 故障注入10个场景全部通过验收准则

文档归档：
□ 各层仿真报告完成归档（版本号、日期、结论）
□ 仿真模型版本与设计版本对应关系记录
□ 标定报告完成（含标定前后对比）
□ HIL测试报告完成（含所有测试用例结果）
```
